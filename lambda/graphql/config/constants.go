package config

const (
	// ServicesLimit set the default limit for returned value
	ServicesLimit = int32(20)
	// ServicesOffset set the default offset for returned value
	ServicesOffset = int32(20)
)
