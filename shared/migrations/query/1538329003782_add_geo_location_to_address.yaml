changeSet:
  - id: '1538329003782'
    author: Enaho Murphy
    description: Add geolocation to `lat` and `long` to the address table
    changes:
      - sql: "
          ALTER TABLE
          `address`
          ADD
            COLUMN `latitude` double DEFAULT NULL,
          ADD
            COLUMN `longitude` double DEFAULT NULL;
        " 